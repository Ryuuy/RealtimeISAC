# 实时 ISAC 开发提示词

把这份文件直接交给新的 Claude Code 会话即可开工。所有数字都是 **2026-08-14 在本机
(`/usr/bin/python3` 3.12.3) 实测**的，不是估算。

---

## 0. 一句话任务

在 USRP B210 上做**不落盘**的实时被动多普勒感知：每 0.02s 出一帧 0.2s 窗的多普勒谱，
终端实时显示，最终只输出「有人 / 无人」。硬约束是**处理速度**和**内存**，不是画面好看。

---

## 1. 先读这个结论：你的两个历史瓶颈都不是真瓶颈

用户过去卡在「内存爆炸」和「计算太慢」，并且认为需要降采样率、可能需要 GPU。
**实测表明：问题出在算法，不在硬件，也不在采样率。**

### 旧设计为什么必然做不到实时

`current/validate_capture_and_caf.py` 在 30 MHz / `DECIM=16` / `Tw=0.2s` 下，
`nperseg = 375,000`：

| | 实测 |
|---|---|
| 单次 375,000 点 FFT | **23.5 ms** |
| 0.02s 预算 | 20 ms |
| 光 FFT 一步 | **已占预算 118%** |

而这 375,000 个输出 bin 里，只有 **240 个**（±600 Hz）被用到——**0.064%**。
算了 375k 点，扔掉 99.94%。这是整件事的核心病灶。

### 新设计：先抽取到 2 kHz，再做 512 点 FFT

`conj(ch0)*ch1` 之后有用信号全部落在 ±500 Hz 内，所以把 **product 流**（不是原始 IQ）
低通抽取到 2 kHz，再做 512 点 FFT：bin 间隔 3.91 Hz，±500 Hz 占 257 个 bin。

**FFT 成本降低约 1500 倍**（23.5 ms → 0.010 ms）。

### ⚠️ 瓶颈不是 CAF 乘法，是 sc16→float 的格式转换

用户原本认为 `conj(Rx1)*Rx2` 的乘法是最吃计算的，这是**降采样率的根本动机**。
实测把 30 MHz 单步（60 万样本 × 2 通道）拆开：

| 步骤 | 耗时 | 占比 |
|---|---|---|
| 1. sc16 → complex64 定标 | **7.98 ms** | **85.8%** |
| 2. `conj(ch0)*ch1` 乘法 | 0.99 ms | 10.6% |
| 3. boxcar 抽取 | 0.33 ms | 3.6% |
| 4. 512 点 FFT | 0.01 ms | 0.1% |

**CAF 乘法只占 10.6%。真正的瓶颈是格式转换，占 85.8%。**

原因：`recv[0]['re']` 是结构化 dtype 的字段视图，stride=4 字节却只读 2 字节，
跨步访问对 cache 极不友好。改成**把整块 int16 连续转成 float32，再 `view` 成 complex64**
（内存布局 `[re,im,re,im,...]` 恰好就是 complex64），一次连续转换搞定：

| 转换写法 | 耗时 | 加速 |
|---|---|---|
| 结构化字段（现在的写法） | 8.01 ms | — |
| 连续块转换 + view | 2.09 ms | **3.8×** |
| 连续块转换，定标推迟到最后 | 1.94 ms | **4.1×** |

### 实测：30 MHz 全带宽的完整单步流水线

| 写法 | 单步耗时 | 预算占用 | 余量 |
|---|---|---|---|
| 朴素（结构化字段 + 物化中间数组） | 11.66 ms | 58.3% | 1.7× |
| **优化（连续转换 + einsum 融合乘法/抽取）** | **4.06 ms** | 20.3% | **4.9×** |
| **优化 + TDD 门控（只算 ON 的 50%）** | **1.49 ms** | 7.5% | **13.4×** |

**⚠️ 结论：保持 30 MHz 全带宽，优化后余量 5×，加 TDD 门控后 13×。
完全不需要降采样率。**

这很重要，因为发射信号带宽 **> 30 MHz**（用户确认）。`conj(ch0)*ch1` 本质是
相关/匹配滤波，输出 SNR 正比于捕获到的信号能量——降到 1 MHz 会丢掉 97% 的信号带宽。
**降采样率是在用大量 SNR 换取根本不需要的计算余量。**

### 核心思想：信息真实速率是 1 kHz，数据速率是 10–30 MHz

TDD 每 1 ms 才发一次 → **每毫秒只能得到一次信道观测**。所以把每个 TDD 帧塌缩成
**一个复数**不丢任何信息（物理决定，非近似）。整个加速就是**尽早压掉这 10⁴ 倍差距**：

```
旧: 600,000样本 →抽取16→ 37,500 →── 375,000点FFT ──→ 只用240个bin (0.06%)
新: 200,000样本 →TDD门控→ 100,000 →每帧积分→ 20个数 →256点FFT→ 256个bin全有用
```

四条措施，按收益排序：

| # | 做法 | 收益 |
|---|---|---|
| 1 | **每 TDD 帧塌缩成一个数** | FFT 375,000 点 → 256 点，**1500×** |
| 2 | **TDD 门控** | OFF 段是噪声×噪声，扔掉一半 |
| 3 | **融合乘法与求和** `einsum('ij,ij->i')` | 不物化中间乘积数组，省一整趟内存往返 |
| 4 | **连续读内存 / 干脆不转换** | `recv['re']` 每 4 字节只读 2 字节，cache 极不友好 |

### ⭐ 不需要转 float：int16 直接算，而且更精确

实测（10 MHz，20 个 TDD 帧 × 5000 ON 样本）：

| 方案 | 耗时 | 正确性 |
|---|---|---|
| A. 结构化字段 + 物化乘积（现在的写法） | 2.44 ms | ✅ |
| B. 连续转 float32 + einsum | **0.72 ms** | ✅ |
| C. 整数 MAC，**int32 累加** | 1.27 ms | ❌ **溢出，结果错** |
| D. **int16 直接 einsum，int64 累加** | **0.78 ms** | ✅ 精确无舍入 |

**转换不是必需的。** 推荐 **D**：全程整数，只把每 step 的 **20 个输出值**转 float
——转换量减少 10⁴ 倍。

⚠️ **必须 `dtype=np.int64` 累加。** C 方案踩的坑：5000 个乘积每个最大
32767² ≈ 1.07e9，int32 上限 2.1e9，**立刻溢出且不报错**。

⚠️ UHD 的 `recv()` 需要 itemsize=4 的缓冲才认 sc16，所以用**结构化 dtype 分配、
再 `.view(np.int16)` 拿连续视图**处理——同一块内存，零拷贝，两边都满足。

### 实测：完整流水线（10 MHz，含盲检测 TDD）

| 步骤 | 耗时 |
|---|---|
| 盲检测 TDD 上沿（折叠平均 20 周期） | 0.088 ms |
| CAF + 帧积分 + FFT + 找峰值 | 0.720 ms |
| **单步合计** | **0.808 ms**（预算 20 ms，**余量 25×**）|
| 进程 RSS | 39 MB |

### 延迟预算

| | |
|---|---|
| 更新频率 | **50 次/秒**（每 20 ms 一个新谱）|
| 端到端延迟 | **~25 ms**（USB ~3ms + 攒满 step 20ms + 计算 0.8ms）|
| 动作 → 谱上可见 | **~0.2 s**（**窗长决定，不是算力决定**）|
| 动作 → "有人"判决 | **~0.3–0.5 s**（加 M-of-N 去抖）|

算力余量 25×，与其空着不如花在能力上：加长 Tw 提频率分辨率，或多扫几个 delay bin。

### 内存实测

| 项 | 实测 |
|---|---|
| DSP 状态（ring + window + FFT buf） | **8.7 KB** |
| 含 recv 缓冲与中间数组（1 MHz） | 634 KB |
| 进程峰值 RSS（含 numpy import） | **32 MB** |

ring buffer 只存**抽取后**的 product 流：0.2s × 2 kHz = 400 个 complex64 = **3.1 KB**。
旧代码是 memmap 整个几 GB 的文件——这就是内存爆炸的来源。稳态下新设计不分配任何东西。

### 不要用 GPU

FFT 只要 0.01 ms，元素级操作是 memory-bandwidth-bound 而非 compute-bound。
GPU kernel 启动 + PCIe 往返延迟（50–200 µs）本身就和整个 CPU 计算时间同量级，
而且要先把 4.6 MB/step 的原始数据搬过去——纯亏。**numpy 够了，余量 5–13 倍。**

（真要用「矩阵加速」，是未来同时扫多个 delay bin 做距离-多普勒图的时候：
把多个 delay 堆成二维数组、沿轴批量 FFT。那仍然是 CPU 上的 numpy，不是 GPU。）

### B210 的 FPGA：有，但用不上

B210 上是 **Spartan-6 XC6SLX150**。但把 FFT/CAF 卸载到它上面**不现实**：

| 障碍 | 事实 |
|---|---|
| **无 RFNoC** | RFNoC（UHD 的用户 FPGA 模块框架）只支持 X3xx/X4xx/E3xx/N3xx。**B2xx 系列不支持**。本机 `/usr/local/share/uhd/images/` 下 RFNoC 镜像数 = **0** |
| **DSP 资源已满** | 官方 `usrp_b210_fpga.rpt`：**DSP48A1 用了 152/180 = 84%**，LUT 40%。只剩 28 个 DSP48，一个复数乘法器就要 3–4 个 |
| **工具链已停产** | Spartan-6 只能用 Xilinx **ISE 14.7**（2013 年停止更新），在现代 OS 上跑都费劲。要改就得写 Verilog 进 b200 代码库重新综合 |
| **出口仍是 USB** | 就算算完了，结果还得过 USB 3.0 回来，延迟和复杂度都爆炸 |

**你已经在免费用 FPGA 硬件加速了**：`usrp.set_rx_rate()` 设成低于主时钟时，
FPGA 里的 DDC（CIC + 半带滤波器链）就在做硬件抽取，只有抽取后的数据过 USB。
这是 B210 唯一实用的硬件加速，而且它加速的正是「抽取」——但我们的场景需要
全带宽进来做 CAF，所以恰好用不上。

**结论：软件优化后余量已有 5–13 倍，没有任何理由碰 FPGA。**

---

## 2. 硬约束（来自用户，不要擅自放宽）

| 约束 | 值 |
|---|---|
| 窗长 `Tw` | 0.2 s |
| 步进 `step` | 0.02 s（重叠 90%，滑窗「撇一部分旧的、拿一部分新的」） |
| 单步预算 | < 0.02 s |
| 多普勒范围 | −500 … +500 Hz |
| 存储 | **零**。不存 IQ、不存谱、不存历史。用完就扔 |
| 显示 | 最快的方式，不要好看的渲染。终端打印即可 |
| 最终输出 | 「有人 / 无人」二值 |
| 分辨率 | 可以降，速度优先 |

---

## 3. 架构

### ⭐ 先理解 TDD 结构——它决定了整个架构

发射信号是 **TDD：0.5 ms 发 / 0.5 ms 不发**，周期 1 ms → **帧率 1 kHz**。

这意味着信道每 1 ms 才被观测一次，所以**多普勒的不混叠上限就是 ±500 Hz**
（1 kHz 的 Nyquist）。这正是用户选 ±500 Hz 的原因，超出就是伪影。

**由此得到的正确架构：每个 TDD 帧只输出一个样本。**

- 只在 ON 的 0.5 ms 上积分 `conj(ch0)*ch1` → 每帧一个复数输出
- 输出率 **恰好 1 kHz**，Nyquist **恰好 ±500 Hz**，与需求完美吻合
- OFF 段是纯噪声×噪声，**不参与计算**——省一半算力，同时提升 SNR
- 消除 OFF 段引入的 1 kHz 方波调制（这是伪影的另一个来源）

对比「无脑抽取到 2 kHz」：那样每个输出点恰好是半个 TDD 周期，
ON/OFF 交替进入，既引入噪声又制造 1 kHz 谱线。**必须做门控。**

### 数据流（每个样本只处理一次）

```
UHD recv (sc16, 预分配缓冲, 30 MHz × 0.02 s = 600,000 样本/通道)
  │
  ├─ 突发门控              用 |ch0|² 找 TDD 的 ON 边沿，只取 ON 段
  │                        (|ch0|² 顺便就是第 8 节要的 Rx 功率监控)
  │
  ├─ int16 → float32       np.copyto(f32buf, raw, casting='unsafe')  ← 连续块！
  │                        再 .view(np.complex64)。别用 c.real = recv['re']（慢 4×）
  │
  ├─ 每帧积分              reshape(n_frame, n_on) 后
  │  conj(ch0)·ch1 求和    np.einsum('ij,ij->i', a.conj(), b)
  │                        ← 融合乘法与求和，不物化中间数组
  │                        每 0.02 s 得到 20 个输出点（20 个 TDD 帧）
  │
  ├─ DC 抑制               EMA 跟踪均值后相减（O(1) 内存，见第 5 节）
  │
  ├─ push 进 ring buffer   0.2 s × 1 kHz = 200 个 complex64 = 1.6 KB
  │                        （滑窗：覆盖最旧的 20 个）
  │
  ├─ blackman(200) × 补零到 256 → FFT → fftshift → |·| → dB
  │                        bin 间隔 3.9 Hz，256 个 bin 铺满 ±500 Hz
  │
  └─ 显示 / 检测           ← 绝不能阻塞
```

**突发同步注意**：TDD 时钟与 USRP 采样时钟不同源，会缓慢漂移。
需要一个轻量的帧相位跟踪器（用 |ch0|² 的上升沿做锁相），并定期重同步。
先用简单的功率阈值找边沿跑通，漂移问题在阶段 1 的验收里观察。

**为什么是「先抽取再进 ring」而不是「ring 存原始 IQ」**：
后者每步要把同样的 0.2s 数据重算 10 遍（90% 重叠），且 ring 要 3.2 MB。
前者每个样本只碰一次，ring 只要 3.1 KB。**计算省 10 倍，内存省 1000 倍。**
这是整个设计里最重要的一个决定，不要改。

### 零分配要求（这台机器内存差，务必遵守）

- 循环外一次性 `np.empty` 分配所有缓冲，循环内**只用 `out=` 形式**
  （`np.multiply(a, b, out=c)`、`np.divide(x, s, out=x)`、`np.conjugate(a, out=a)`）
- ⚠️ 注意：`c0 /= 32767.0` 在函数里会让 Python 把 `c0` 当局部变量而报
  `UnboundLocalError`——写基准脚本时已经踩过。用 `np.divide(c0, 32767.0, out=c0)`
- 不要有会增长的 list / deque / np.append
- 热循环里不要 import matplotlib（它本身就吃几十 MB）
- 建议加一个每 N 帧检查 `resource.getrusage().ru_maxrss` 的断言，确认 RSS 是平的

---

## 4. 显示：用最快的，别用好看的

实测（257 个 bin → 终端）：

| 方式 | 耗时 | 预算占用 |
|---|---|---|
| 单行峰值 f-string + `\r` | **3.7 µs** | 0.019% |
| ASCII 瀑布行（65 字符 ramp） | **29.5 µs** | 0.148% |
| matplotlib blitting | ~1–5 ms | 5–25% |
| matplotlib 朴素重绘 | 50–200 ms | **超预算** |

**两种终端方式都是白菜价，瀑布图完全跑得起。** 默认实现：

1. **主输出**：单行 `\r` 覆盖，打印峰值多普勒 + 幅度 + 检测结果
2. **可选 `--waterfall`**：每帧输出一行 ASCII/ANSI 瀑布，50 行/秒滚动，
   这就是用户要的「视频」，而且比 matplotlib 快两个数量级

**绝对不要**在热循环里用 matplotlib。如果将来一定要图形界面，
必须放到独立进程 + 丢帧（显示最多 10–20 fps，DSP 照跑 50 fps），且永不阻塞 DSP。

---

## 5. DC 问题（用户明确提到）

`conj(ch0)*ch1` 对静态信道产生一个常量 → 全部能量堆在 0 Hz，而且非常大。
用户要求「不断更新阈值但不储存历史」，正好对应 O(1) 内存的做法：

```python
dc = (1 - a) * dc + a * dec.mean()   # a ≈ 0.01，单极点 IIR
np.subtract(dec, dc, out=dec)
```

再叠加一个**保护带**：找峰值和 CFAR 训练时都排除 |f| < 5–10 Hz。

两个都要做：EMA 压掉稳态 DC，保护带兜住残留。

---

## 6. 从现有代码继承什么（诚实版：几乎全部重写）

用户原本的想法是「以 `validate_capture_and_caf.py` 为核心重构」。
但在**不落盘 + 实时**的前提下，那份代码是彻底 file-first 的批处理管线，
**共享的是数学，不是代码**。处置表：

| 现有代码 | 处置 |
|---|---|
| `USRPSaveData2Channel.py:89-124` 设备配置块 | ✅ **照抄**。这段 UHD 咒语很难调对，是最有价值的遗产 |
| `validate_capture_and_caf.py:75-86` sc16 定标 (`/32767`) | ✅ **保留数学**，丢掉 memmap |
| `:209` `conj(ch0)*ch1` | ✅ 保留概念 |
| `:212-216` blackman + FFT + fftshift + dB | ✅ 保留概念，尺寸从 375k 改成 512 |
| `acquire_and_save` 的 recv 循环 (`:137-160`) | ❌ **重写**。删掉 `f.write`，改 recv → 处理回调 |
| `create_experiment_folder` / `save_acquisition_parameters` | ❌ **删除**。不存任何东西 |
| `read_2ch_iq_memmap` / `check_data_format` / `load_acquisition_parameters` / `find_latest_experiment_folder` | ❌ **删除**。没有文件了 |
| `compute_pdp_delay_memmap` | ⚠️ **保留思想，重写实现**。30 MHz 下 delay 仍有意义（5 m 分辨率），但改成定期更新而非每帧重算 |
| `build_decimated_channel`（跨步切片） | ❌ **替换**成 TDD 帧内积分（跨步切片没有抗混叠，是错的） |
| `USRPSaveData2Channel.py:118` `recv_buffer` 用结构化 dtype | ⚠️ **改**成扁平 int16 缓冲 + `view`，否则转换慢 4×（见第 1 节） |
| `fast_caf_spectrogram` / `..._at_delay` | ❌ **重写**成增量式 |
| `check_iq_magnitude_abort_memmap` | ❌ **改写**成每 chunk 滚动计数 |
| `range_step_for_sample_rate` / `range_label_for_delay` | ❌ **删除**（低采样率下没有距离分辨率） |
| `run_validation` / `run_validation_gif` (:263-514, 共 250 行) | ❌ **删除** |

**⚠️ 这也意味着 `REFACTOR_NOTES.md` 里那份 7 步重构计划优先级要下调**：
`formats/`、`io/experiment_store`、离线 viz 在「不落盘」前提下基本作废。
唯一仍然值得保留的是 **D3（合并两个 CAF 函数）** 的思想——实时内核必须只有一份。

**但建议保留一个薄的离线回放路径**：读一份已录的 `.bin`，喂给**同一个** DSP 内核，
用来验证实时结果与已知正确的批处理结果一致。这是唯一值得为「文件」保留的东西，
而且它只是测试工具，不进实时链路。

### ✅ 保持 30 MHz 的额外好处：距离分辨率保住了

| fs | 1 个 delay bin 对应距离 |
|---|---|
| **30 MHz（保持）** | **5.0 m** |
| 1 MHz | 150 m |
| 500 kHz | 300 m |

这是不降采样率的顺带收益。当前目标（有人/无人）只需要多普勒，
但 5 m 的距离分辨力意味着**将来可以做距离-多普勒图而不用改采集链路**——
到时把多个 delay 堆成二维数组沿轴批量 FFT 即可（仍是 CPU numpy，见「不要用 GPU」）。

因此 `compute_pdp_delay_memmap` 的**思想要保留**（选一个最强 delay 做 CAF），
只是实现要改成在实时流上定期更新，而不是每帧重算。

---

## 7. 开工前必须回答的问题

**Q1（已解答，2026-08-14）：发射信号带宽 > 30 MHz，TDD 0.5 ms 发 / 0.5 ms 不发。**
→ **保持 fs = 30 MHz，不降采样率。** 见第 1 节实测。
（早先「1.89 GHz 是 DECT，用 2 MHz」的推测是错的：1.89 GHz 只是外部毫米波前端下变频
后的 IF 接口频率，不是真实载波。）

**Q1b（新的阻塞性问题）：毫米波前端的实际载波频率是多少？**
不影响代码结构，但**决定多普勒→速度的换算，以及 ±500 Hz 会不会混叠**。
频率变换不改变多普勒的 Hz 值，所以你在 1.89 GHz IF 上看到的就是毫米波尺度的多普勒。
按 `f_d = 2v/λ = 2vf/c` 算走路（1.5 m/s）：

| 载波 | λ | 走路 1.5 m/s 的 f_d | 是否超 ±500 Hz |
|---|---|---|---|
| 24 GHz | 12.5 mm | 240 Hz | 安全 |
| 28 GHz | 10.7 mm | 280 Hz | 安全 |
| 60 GHz | 5.0 mm | **600 Hz** | ⚠️ 已超 |
| 77 GHz | 3.9 mm | **770 Hz** | ⚠️ 已超 |

TDD 帧率 1 kHz 决定了不混叠上限就是 ±500 Hz（硬上限，改不了）。
若载波是 60/77 GHz，**正常走路就会混叠**——这不是 bug 是系统限制，
但必须知道，否则会把混叠回来的分量当成真实目标。

**Q2：boxcar 抽取的混叠可以接受吗？**
boxcar 第一旁瓣只有 −13 dB，带外内容会折叠进来。先用 boxcar（最快、最简单），
在真实数据上看噪底是否异常抬高；若有问题再升级成两级抽取或 polyphase FIR。
（boxcar 在 ±500 Hz 边缘的幅度下垂：抽到 2 kHz 时 −0.9 dB，抽到 4 kHz 时 −0.22 dB。
若在意平坦度，抽到 4 kHz 后只用内侧 ±500 Hz。）

**Q3：单线程还是生产者/消费者？**
处理只占预算 1.8–3.4%，**先做单线程**，最简单、最容易验证。
只有当日志里出现 UHD overflow (`O`) 才引入 recv 线程 + 无锁 ring。不要过早优化。

---

## 8. 分阶段实施

### 阶段 1 — 实时多普勒（现在只做这个）

1. `rt_config.py`：`fs=30e6`、`Tw=0.2`、`step=0.02`、`tdd_period=1e-3`、`tdd_duty=0.5`、
   `fs_out=1000`（=TDD 帧率）、`NFFT=256`、多普勒范围 ±500
2. `rt_source.py`：USRP 取流。设备配置照抄 `USRPSaveData2Channel.py:89-124`，
   `st_args` 保持 `sc16/sc16`，**`num_recv_frames=1024` 保留**（30 MHz×2ch=240 MB/s，
   接近 B210 USB3 上限，缓冲不能减）。循环里**不写文件**，把 chunk 交给回调。
   ⚠️ recv 缓冲用扁平 int16 而非结构化 dtype
3. `rt_sync.py`：TDD 突发检测。用 |ch0|² 找 ON 边沿 + 帧相位跟踪（应对时钟漂移）
4. `rt_dsp.py`：`DopplerEngine` 类，持有全部预分配缓冲和 ring/DC 状态，
   `push(chunk) -> spectrum | None`（攒够 step 才出一帧）
5. `rt_display.py`：单行峰值打印 + 可选 ASCII 瀑布
6. `rt_main.py`：串起来，加一个每帧的耗时统计（p50/p99）和 RSS 监控

### ✅ 阶段 1 已完成并在真机验证（2026-08-14）

代码在 [`realtime/`](realtime/)，详细实测数据与踩坑记录见
[realtime/README.md](realtime/README.md)。摘要：

| fs | 步数/时长 | p50 | p99 | 余量 | overflow | RSS |
|---|---|---|---|---|---|---|
| **10 MHz** | 3000 / 60s | **4.07 ms** | **5.86 ms** | **3×** | **0** | 60 MB（无泄漏）|
| 30 MHz | 1214 / 25s | 9.24 ms | 14.57 ms | 1× | 51 ⚠️ | 92 MB |

**关键修正：必须用生产者线程。** 本文早先的 Q3 说"先做单线程"——实测证明**做不到**：
recv() 是实时节拍的（拉 20ms 数据就要等 20ms），单线程再串 4–6ms 的 DSP
一轮就是 25ms > 20ms 预算，天生落后 25%。单线程 30 秒 **781 次 overflow**，
加生产者线程后 **0 次**。

尚未验证（需要接上 TX + 毫米波前端）：TDD 实际锁定、帧同步抗漂移、
人走过时能否看到多普勒。目前是自由运行模式跑通的。

⚠️ 30 MHz × 2ch = **240 MB/s**，接近 B210 USB 3.0 的实用上限，
overflow 风险主要来自这里而不是计算。若持续 overflow，优先排查 USB 控制器/线缆，
再考虑降到 20 MHz——**但降之前先确认不是 USB 问题**，因为计算侧余量还有 5–13 倍。

### 阶段 2 — 检测（未来，先不做）

用户设想：CFAR 或基于 power 的卡尔曼，不断更新阈值但不存历史。建议：

- **Rx1 功率监控**：`|ch1|²` 的 EMA，O(1)。用来发现天线掉了/增益不对/信号源停了
- **CA-CFAR**（沿多普勒轴）：每个 bin 取两侧 N 个训练单元、G 个保护单元求均值，
  阈值 = α × 噪声估计。用 `np.cumsum` 技巧做到 O(N) 而不是 O(N·M)。
  **训练单元和被测单元都要排除 DC 保护带（|f| < 5–10 Hz）**
- **跨帧稳定**：噪底再叠一层慢速 EMA，仍是 O(1) 内存
- **判决**：统计保护带外超阈值的 bin 数 → **M-of-N 去抖**（一个小整数计数器，
  不需要历史数组）→ 输出布尔值
- **关于卡尔曼**：对一个二值判决来说是过度设计。EMA 跟踪噪底 + CFAR + 迟滞
  能达到同样效果且状态只有几个标量。除非有明确证据，否则不建议上卡尔曼

**物理参考**：⚠️ **1.89 GHz 不是真实载波**，它只是外部毫米波前端下变频后的 IF。
多普勒由**毫米波载波**决定（频率变换保持多普勒的 Hz 值不变）。
按 `f_d = 2v/λ` 算，人体运动在毫米波下**远超 ±100 Hz**，
所以用户选 ±500 Hz 是对的——具体数值取决于载波，见第 7 节 Q1b 的表。

⚠️ 由于 TDD 帧率 1 kHz，**±500 Hz 是硬性不混叠上限**。若载波够高（60/77 GHz），
正常走路就会超出这个范围而混叠。检测逻辑要考虑到快速运动可能出现在"错误"的频率上。

频率分辨率 1/Tw = **5 Hz**（TDD 门控后：0.2 s × 1 kHz = 200 点，补零到 256 时
bin 间隔 3.9 Hz）。够做有无检测；若将来要区分动作类型需要加长 Tw，
代价是时间分辨率下降。

---

## 9. 环境（不要踩）

**必须用 `/usr/bin/python3`（3.12.3），不能用默认的 `python3`。**
默认 `python3` 是 miniconda 3.13.5，没有 uhd；手动加 PYTHONPATH 会撞
`GLIBCXX_3.4.32 not found`（conda 自带 libstdc++ 太旧）。

UHD 4.8.0.HEAD-0-g308126a4，B210，`serial=321D889`，中心频率 1.89 GHz，增益 30。
numpy 1.26.4 / scipy 1.11.4。
